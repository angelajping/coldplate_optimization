# coldplate_optimization

"""
cold_plate_model.py    <- physics equations only (Re, f, Nu, h_conv, dP)
sweep.py                <- generates the grid, runs the model, filters, finds Pareto front
plot_results.py         <- visualization
main.py                 <- ties it together
"""